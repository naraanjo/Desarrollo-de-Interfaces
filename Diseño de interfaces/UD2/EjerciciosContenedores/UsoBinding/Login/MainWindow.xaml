<Window x:Class="Login.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Login"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">



    <Window.Resources>
        <Style x:Key="BotonRedondeado" TargetType="Button">
            <Setter Property="Background" Value="#4FC3F7"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F4B400"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="true">
                    <Setter Property="Background" Value="#F1CA68"/>
                </Trigger>
            </Style.Triggers>
        </Style>



        <Style x:Key="BotonRedondeado2" TargetType="Button">
            <Setter Property="Background" Value="Red"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F4B400"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="true">
                    <Setter Property="Background" Value="#F1CA68"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style x:Key="Titulo" TargetType="Label">
            
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            
            <Style.Triggers>
                
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#F4B400"/>
                </Trigger>
                
            </Style.Triggers>
            
        </Style>


    </Window.Resources>


    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

        <Label Style="{StaticResource Titulo}" Content="Sign in"
       HorizontalAlignment="Left" Height="48" Margin="0,62,0,0"
       VerticalAlignment="Top" FontSize="28"
       FontWeight="Bold"  Width="440"/>



        <TextBlock HorizontalAlignment="Center" Height="38" Margin="0,30,0,0" Text="Username"
                       TextWrapping="Wrap" VerticalAlignment="Top" Width="362" FontSize="26"
                       Background="#FFF6F6F6" Foreground="DarkGray" TextAlignment="Center">

        </TextBlock>

        <TextBox HorizontalAlignment="Center" Height="38" Margin="0,20,0,0" Text="Password"
                     TextWrapping="Wrap" VerticalAlignment="Top" Width="362" FontSize="26" Background="#FFF6F6F6"
                     Foreground="DarkGray" TextAlignment="Center"></TextBox>


        
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Style="{StaticResource BotonRedondeado}" Content="Login" Width="170" Height="50" Margin="0,50,0,0"
                    FontSize="16" FontWeight="SemiBold" Foreground="White"></Button>

                <Button Grid.Column="1" Style="{StaticResource BotonRedondeado2}" Content="Login" Width="170" Height="50" Margin="0,50,0,0"
                    FontSize="16" FontWeight="SemiBold" Foreground="White"></Button>
        
        </Grid>
        
    </StackPanel> 
</Window>
